# 分销平台环境变量配置示例

# 是否使用 Mock 数据（开发环境默认 true，生产环境需显式配置）
API_USE_MOCK="true"

# 部署环境标识
DISTRIBUTOR_ENVIRONMENT="development"

# Node 环境
NODE_ENV="development"

# Sentry DSN（可选，用于错误监控）
SENTRY_DSN=""

# 日志上报端点（可选）
DISTRIBUTOR_LOG_ENDPOINT=""
